name: subiquity_test
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  build_runner: ^2.2.0
  mockito: 5.4.4
  path: ^1.8.3
  subiquity_client: ^0.1.0

dev_dependencies:
  test: any
  ubuntu_lints: ^0.3.0
