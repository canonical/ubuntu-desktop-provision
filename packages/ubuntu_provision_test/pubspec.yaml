name: ubuntu_provision_test
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  ubuntu_flavor: ^0.2.0
  ubuntu_provision:
    path: ../ubuntu_provision
  ubuntu_test: ^0.1.0-0
  ubuntu_wizard:
    path: ../ubuntu_wizard
  yaru_test: ^0.1.3
  yaru_widgets: ^2.5.0
