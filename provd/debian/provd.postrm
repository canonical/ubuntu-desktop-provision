#!/bin/sh

set -e

case "$1" in
    purge)
        # Remove gnome-initial-setup from the gnome-initial-setup group
        usermod -a -G gnome-initial-setup nogroup
        ;;
esac

#DEBHELPER#

exit 0
