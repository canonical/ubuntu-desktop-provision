syntax = "proto3";

option go_package = "github.com/canonical/ubuntu-desktop-provision/provd/internal/daemon/testdata/grpctestservice";

package grpctestservice;

service TestService {
    rpc Blocking (Empty) returns (Empty) {}
}

message Empty {
}
