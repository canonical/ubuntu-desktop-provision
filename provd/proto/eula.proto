syntax = "proto3";

package eula;

// Just a placeholder until we have designed the EULA
service EULAService {
    rpc AcceptEULA(AcceptEULARequest) returns (Empty) {}
}

message Empty {}

message AcceptEULARequest {
    string eulaId = 1;
    string username = 2;
}
